# Aster 项目复审整改实施计划

1. 修复 Tauri capability、窗口错误通道和 CSP。
2. 建立通用安全凭证命名空间，迁移 SMTP 密码与客户端 token，清理快照和备份。
3. 建立本地日期公共方法，替换重复 UTC 月份实现。
4. 收紧密码重置、设置保存和配对写入的事务与锁范围。
5. 引入 Vitest、Playwright，补日期、窗口布局与关键状态测试；扩展 Windows CI。
6. 增加定向刷新入口，减少无关模块重复加载；把高数据量列表接到真实分页接口。
7. 新增 SQLite 性能迁移并将月份函数过滤改为范围过滤。
8. 按领域拆分超 500 行文件，迁移内联测试，收紧工程基线。
9. 登录表达动画按需加载，复核构建产物体积。
10. 完整执行本地自动化、桌面实机和最终代码 review。
